# Optional overlay that deploys Scrutineer WITH the ApprovalRequest identity-stamping
# admission webhook enabled. Requires cert-manager in the cluster.
#
#   kustomize build config/webhooks | kubectl apply -f -
#
# The base `config/default` overlay deploys Scrutineer without webhooks (no cert
# dependency), so this is purely additive and opt-in.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: scrutineer-system

resources:
- ../default
- ../webhook
- ../certmanager

patches:
- path: manager_webhook_patch.yaml
  target:
    kind: Deployment
    name: scrutineer-controller-manager
- path: manager_enable_webhooks_patch.yaml
  target:
    kind: Deployment
    name: scrutineer-controller-manager
- path: webhook_cainjection_patch.yaml
  target:
    kind: MutatingWebhookConfiguration
