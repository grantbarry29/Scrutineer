---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: toolpolicies.relay.secureai.dev
spec:
  group: relay.secureai.dev
  names:
    kind: ToolPolicy
    listKind: ToolPolicyList
    plural: toolpolicies
    shortNames:
    - tp
    - toolpol
    singular: toolpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ToolPolicy is a reusable tool/MCP policy that AgentSessions can
          reference.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToolPolicySpec defines reusable tool/MCP governance rules
              for AgentSessions.
            properties:
              allowedTools:
                description: AllowedTools lists tool/MCP identifiers the agent may
                  invoke.
                items:
                  type: string
                type: array
              deniedTools:
                description: DeniedTools lists tool/MCP identifiers the agent must
                  not invoke.
                items:
                  type: string
                type: array
              maxCallsPerMinute:
                description: MaxCallsPerMinute caps tool invocations per minute (MVP
                  declaration only).
                format: int32
                minimum: 0
                type: integer
              maxToolCalls:
                description: MaxToolCalls caps total tool invocations for the session.
                format: int32
                minimum: 0
                type: integer
              mode:
                allOf:
                - enum:
                  - audit-only
                  - dry-run
                  - enforced
                - enum:
                  - audit-only
                  - dry-run
                  - enforced
                default: audit-only
                description: |-
                  Mode controls how tool rules are interpreted once enforcement exists.
                  Defaults to audit-only when unset.
                type: string
            type: object
          status:
            description: ToolPolicyStatus defines the observed state of a ToolPolicy.
            properties:
              observedGeneration:
                description: ObservedGeneration is reserved for a future ToolPolicy
                  controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
