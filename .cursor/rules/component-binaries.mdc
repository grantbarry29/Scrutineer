---
description: Build/deploy conventions for Scrutineer's independently built binaries (manager + dns-proxy/tool-gateway/fs-gateway sidecars) and what their component READMEs must cover.
globs: cmd/**,internal/enforcement/**,Dockerfile*
alwaysApply: false
---

# component-binaries

> **Canonical content lives in [`dev-agent-rules/component-binaries.md`](../../dev-agent-rules/component-binaries.md).**
> This file only carries the Cursor auto-attach metadata (description/globs/alwaysApply) above.
> Read the canonical file before working in scope — do not edit rules here.
