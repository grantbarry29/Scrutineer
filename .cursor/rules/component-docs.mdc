---
description: Require concise, current per-component README docs. Every independently built/deployed/operated component (binary, controller, service, sidecar, worker, CLI) keeps a local README; create/update it in the same change as the code.
alwaysApply: true
---

# component-docs

> **Canonical content lives in [`dev-agent-rules/component-docs.md`](../../dev-agent-rules/component-docs.md).**
> This file only carries the Cursor auto-attach metadata (description/globs/alwaysApply) above.
> Read the canonical file before working in scope — do not edit rules here.
