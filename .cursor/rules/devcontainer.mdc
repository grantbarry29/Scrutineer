---
description: All build/test/codegen runs inside the provided devcontainer (pinned Go 1.23 + tools), never on the host. Host runs of make/codegen/envtest silently break the toolchain and produce false "bugs".
alwaysApply: true
---

# devcontainer

> **Canonical content lives in [`dev-agent-rules/devcontainer.md`](../../dev-agent-rules/devcontainer.md).**
> This file only carries the Cursor auto-attach metadata (description/globs/alwaysApply) above.
> Read the canonical file before working in scope — do not edit rules here.
